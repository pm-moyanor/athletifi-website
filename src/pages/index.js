import BaseLayout from "@/components/layouts/BaseLayout";

// Refactor the Home component so it contains the elements typical of a home page, such as a hero image, a call to action, and a footer.
const Home = () => {
  
  return (
    <BaseLayout>
      <h1>AthletiFi Home Page!</h1>

    </BaseLayout>
  )
}

export default Home;

